# Packages required for testing
-r base.txt

mypy
coverage
ddt
diff-cover
django_dynamic_fixture
# astroid is a dependency of pylint, but we need to pin the version here to
# prevent astroid v2 from being installed, which pylint doesn't support
# (pylint is installed via edx-lint)
astroid>=1.5.1,<1.6
edx-lint
factory_boy
mock
pycodestyle
pytest
pytest-cov
pytest-django
